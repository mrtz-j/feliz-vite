{
    "private": true,
    "scripts": {
        "postinstall": "dotnet tool restore",
        "start": "dotnet fable watch ./src -s --run vite",
        "fsbuild": "dotnet fable ./src --noCache -s --run vite build ./src --mode production --config ./vite.config.js",
        "build": "dotnet fable src -s --run vite build",
        "preview": "vite preview",
        "format": "dotnet fantomas . -i src"
    },
    "devDependencies": {
        "@vitejs/plugin-react": "^3.0.1",
        "autoprefixer": "^10.4.13",
        "daisyui": "^2.47.0",
        "postcss": "^8.4.21",
        "tailwindcss": "^3.2.4",
        "vite": "^4.1.5"
    },
    "version": "1.0.0",
    "dependencies": {
        "react": "^18.2.0",
        "react-dom": "^18.2.0"
    }
}